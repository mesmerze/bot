crm.flip_form('create_system');

- unless params[:cancel].true?
  $('#create_system').html('#{ j render(partial: "new") }');
  $('#empty').html('');
  crm.set_title('create_system', '#{t(:create_system)}');

- else
  crm.set_title('create_system', '#{t(:systems)}');
