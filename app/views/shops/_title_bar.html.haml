#confirm{ hidden }
.title_tools#menu
  = link_to_inline(:edit_shop, edit_shop_path(@shop), text: t(:edit)) + " | "
  = link_to_function(t(:delete) + '?', confirm_delete(@shop))
.title_tools#buttons
  = view_buttons
.title#edit_shop_title
  = h(@shop.name)
  = image_tag("loading.gif", size: :thumb, id: "loading", style: "display: none;")

%div#edit_shop{ hidden }
