#confirm{ hidden }
.title_tools#menu
  = link_to_inline(:edit_org, edit_org_path(@org), :text => t(:edit)) + " | "
  = link_to_function(t(:delete) + '?', confirm_delete(@org))
.title_tools#buttons
  = view_buttons
.title#edit_org_title
  = h(@org.name)
  = image_tag("loading.gif", :size => :thumb, :id => "loading", :style => "display: none;")

%div#edit_org{ hidden }
